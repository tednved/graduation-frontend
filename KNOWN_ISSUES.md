# KNOWN_ISSUES

非阻断问题清单。这里只记录不影响 10 分钟答辩演示、且已有绕行方案的问题；
P0/P1 一律在当轮任务中修复，不进本文件。

## KI-01 分类页二级分类点不进去（P2）

- 现象：在分类页点到二级分类，只弹一句「分类已就绪，商品列表将在后续任务实现」，没有按分类过滤的商品列表。
- 原因：分类页目前只实现到两级浏览，分类维度的商品列表未接通。
- 绕行：找商品走首页或搜索页（可按关键词检索），演示走搜索路径。
- 关闭条件：分类 → 商品列表路由接通后移除本条目。

## KI-02 真机预览时图片与接口不可达（P2）

- 现象：`config/index.js` 的 `baseUrl` 固定为 `http://127.0.0.1:8080/api/v1`，真机预览时 127.0.0.1 指向手机自己，接口与 `utils/media-url.js` 拼出的图片地址都会加载失败。
- 原因：本地演示环境按 AGENTS.md 固定了同机地址，没有做环境切换。
- 绕行：答辩演示用微信开发者工具在同机运行（已验收通过）；确需真机时把 `baseUrl` 改成局域网地址并保持后端可被访问。
- 关闭条件：引入环境化配置（开发/真机/生产）后移除本条目。
