# QA-01 前端交接

- 分支：`fix/QA-01-final`
- PR：[#6](https://github.com/tednved/graduation-frontend/pull/6)（Draft）
- 状态：REVIEW；自动检查、真实 HTTP 与 GUI 复验均已完成
- 裁定：个人订单继续使用 `/users/me/orders` 与 `/orders/{id}`，只认当前账号的买卖身份；全站订单监管只走 `/admin/orders` 与 `/admin/orders/{id}`。

## 实现

- 管理台增加“订单”区域，支持订单状态、订单号/商品标题筛选和分页。
- `services/admin-api.js` 增加全站订单列表与监管详情读取。
- `utils/admin-view.js` 同时映射买家、卖家、商品快照和状态。
- 新增 `pages/admin-order-detail/**`；详情只读，强制清空买卖动作和评价入口。
- 已预订与已售商品隐藏强制下架按钮，与后端 409 守卫保持一致。

## 验证

- `node scripts/check-shell.cjs`：6/6，17 个页面四件套齐全。
- `node --test "tests/**/*.test.js"`：138/138。
- 真实 HTTP 主链（`qa-chain.js`，`IT_RUN=qa-fix-2026`，全新账号）：59 PASS / 0 FAIL / 1 SKIP。SKIP 是「认证证据不可公开读取」——MVP 不开放证据上传，公开 API 造不出越权场景。`/orders/{id}` 对非参与方（含管理员）返回 `ORDER_OPERATION_FORBIDDEN`。
- 开发者工具 GUI 冒烟（`gui-smoke.js`）：90 PASS / 0 FAIL / 0 console 错误，33 张截图，exit 0。管理台五个 Tab 逐一切换；订单区列表 20 行；监管详情 4 行快照 + 时间线、零买卖按钮、只读说明「管理视图只读；请由真实买卖双方处理订单状态。」。`/admin/orders` 与 `/admin/orders/{id}` 就是在这一段经 `services/admin-api.js` 打到真实后端并断言渲染结果；`qa-chain.js` 未直接断言该端点。
