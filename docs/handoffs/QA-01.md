# QA-01 交接报告

## 基本信息

- 任务 ID：QA-01
- 目标：从空库复现完整演示链，清零阻断答辩演示的功能、权限、事务与界面问题
- 执行者：Claude
- 分支：`fix/QA-01-final`
- PR：[#6](https://github.com/tednved/graduation-frontend/pull/6)
- 状态：REVIEW

## 实现结果

- 已完成：管理台「强制下架」按钮的可见性收紧。`utils/admin-view.js` 的 `offShelfable` 从「非已下架、非已删除」改为「仅在售或草稿」。原因是已预订（有进行中订单）与已售出的商品一旦被强制下架，订单就再也走不完——后端已同步把这两种状态改为返回 409 `ITEM_NOT_EDITABLE`，前端不能再把按钮渲染出来让管理员点了才知道。
- 未完成：无

## 修改文件

| 文件 | 用途 |
| --- | --- |
| `utils/admin-view.js` | `offShelfable` 收紧为「仅在售/草稿」，并写清理由 |
| `tests/admin/admin-view.test.js` | 补 `DRAFT` 可下架、`RESERVED`/`SOLD` 不可下架的断言 |

## 接口与配置变化

- 依赖后端任务：QA-01（后端 PR `tednved/graduation-backend#8`）
- OpenAPI 版本：3.1，无版本号变更；`/admin/items/{id}/off-shelf` 新增 `409` 响应
- 新增或修改 service：无
- app.json/env/constants 变化：无

## 页面规则

- 登录与权限：管理台入口仍按 `GET /users/me` 的 `role` 判定，未改动。
- 加载/空态/错误态：未改动。已预订/已售出的商品卡片不再渲染操作区，状态、锁定标记与下架原因照常展示；页面数据过期时按后端的 409 提示即可。
- 防重复操作与分页：未改动。

## 验证证据

| 开发者工具场景 | 结果 |
| --- | --- |
| `node scripts/check-shell.cjs` | PASS 6/6 |
| `node --test "tests/**/*.test.js"` | PASS 137/137，含改后的「管理端商品视图」用例 |
| 开发者工具：管理台商品 Tab 不再对已预订商品渲染「强制下架」 | 未运行及原因：负责人指示跳过修复后复验。修复前基线 72 PASS / 0 FAIL / 0 console 错误（25 张截图） |
| 开发者工具：评价页（创建/已评价空态/缺参失败态/公开评价页） | 未运行及原因：同上。覆盖已补入冒烟脚本，脚本未执行 |

## 联调结果

- 后端环境：`http://127.0.0.1:8080/api/v1`，独立验收库 `graduation_qa01`，Mock 登录（多账号）
- 成功路径：修复前真实 HTTP 主链 60 PASS / 0 FAIL / 1 SKIP（登录、认证审核、发布、搜索、收藏、下单、接单、交付、收货、评价、消息已读、用户禁用/恢复、商品强制下架、审计查询）
- 失败路径：以真实 HTTP 复现并锁定「强制下架已预订商品 → 订单永久卡死」，服务端守卫由后端 PR 修复
- 尚未联调内容：修复后的真实链路与开发者工具复验（负责人指示跳过）

## 风险与阻塞

- 已知风险：无
- 阻塞事项：修复后的真实链路与开发者工具复验未执行，需以新代码重启后端
- 需要负责人决定：无

## 下一任务输入

- 可复用组件或 service：`utils/admin-view.js` 的各 `build*View` 是管理台唯一的展示层映射，管理端新增操作按钮时应在同一处计算可见性，不要把状态判断散进 wxml。
- 页面路由与数据结构：管理台 `pages/admin/admin` 四个 Tab（认证审核/用户/商品/审计），商品条目依赖后端 `ItemStatus`
- 接入注意事项：操作按钮的最终判据永远在服务端；前端隐藏只是避免让用户点一次必然失败的按钮。
